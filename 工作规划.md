# 文物数据库与自动化历史内容生产系统 —— 工作规划

> 版本：v0.3（开工版）
> 日期：2026-09-04
> 状态：**已开工，P0 进行中**

---

## 1. 项目愿景

构建一个**聚合全球博物馆公开数据的中文文物数据库**，并在此之上运行一条**全自动的每日内容生产流水线**：

```
每日热点 → 选题Agent → 文物专家Agent → 编辑Agent → 有趣的历史推文
└────────────── 总协调Agent 统一调度与进度控制 ──────────────┘
              ↓ 检索              ↓ 检索
          [文物数据库 + 历史/考古知识库]
```

核心特色：**不做单件文物的介绍，而是"文物组合"叙事**——用一组相互关联的文物，还原一种历史生活状态或一个历史事件，进而联系当下、探讨人性。

### 1.1 三大交付物

| 交付物 | 形态 | 说明 |
|---|---|---|
| ① 文物数据库 | GitHub 开源仓库 | 结构化数据（JSON/SQLite）+ 公开图片链接 |
| ② 检索前端 | 静态网页（GitHub Pages） | 按名称/朝代/材质/主题/博物馆搜索浏览 |
| ③ Agent 流水线 | 每日自动运行 | 选题 → 专家意见 → 成稿，产出图文推文 |

---

## 2. 总体架构

```
┌─────────────────────────────────────────────────────┐
│                   数据采集层（每日增量）                  │
│  大陆博物馆 / 海外博物馆 / 港台博物馆 / 学术公开数据           │
└──────────────────────┬──────────────────────────────┘
                       ↓ 清洗、去重、统一Schema、打标签
┌─────────────────────────────────────────────────────┐
│              文物数据库（GitHub 仓库为核心存储）            │
│  data/*.json + images索引 + SQLite快照 + 全文检索索引     │
└─────────┬───────────────────────────────┬───────────┘
          ↓                               ↓
┌─────────────────────┐      ┌──────────────────────────┐
│  检索前端（静态站点）   │      │ Agent 内容流水线（每日1篇）  │
│  GitHub Pages 部署   │      │ ⓪总协调Agent（调度+状态）    │
└─────────────────────┘      │ ①选题Agent ← 热点源        │
                             │ ②文物专家Agent ← RAG知识库  │
                             │ ③编辑Agent → 公众号+小红书   │
                             │ ④审校质检 → ⑤人工过目后发布  │
                             └──────────────────────────┘
```

---

## 3. 子系统一：文物数据库

### 3.1 数据来源清单（按优先级）

**A. 有公开 API / Open Access 的（优先接入）**

| 机构 | 接入方式 | 图片授权 |
|---|---|---|
| 台北故宫博物院 | Open Data 专区 API | 开放授权，优先接入 |
| 美国大都会博物馆 (Met) | Open Access API | CC0 |
| 克利夫兰艺术博物馆 | Open Access API | CC0 |
| 史密森尼学会 | Open Access API | CC0 为主 |
| 哈佛艺术博物馆 | API | 开放授权 |
| 大英博物馆 | API / SPARQL | 需逐条核对授权 |
| 东京国立博物馆等日本馆 | 部分开放数据 | 需核对 |
| Wikidata / 各馆联合目录 | SPARQL | 结构化补充 |

**B. 无 API、采用"浏览器实开 + 截图 + OCR"采集的（二期）**

| 机构 | 说明 |
|---|---|
| **中国国家博物馆（官网）** | **B 类截图采集试点馆（已确认）**：实开网页逐件截图 + OCR 采集，跑通后复制方法论到其他大陆大馆 |
| 故宫博物院（北京）数字文物库、上海博物馆、南京博物院、陕西历史博物馆、三星堆博物馆等 | 逐馆评估页面结构 |
| 香港故宫文化博物馆、香港历史博物馆 | 数据较少，列入补充源 |

**B 类采集方式（明确不做 HTTP 爬虫）**：这些网站多有反爬设置，因此完全模拟人工浏览：

1. 用浏览器自动化（Playwright 有头模式）**真实打开网页**，按人的浏览节奏操作
2. **页面文字**：整页截图 → OCR 识别入库（RapidOCR；本机 document-extraction 技能已有成熟经验）
3. **文物图片**：点击图片打开放大视图，**截取放大后的大图**（而非列表页缩略图）
4. 图片**带水印可接受**，`credit` 字段强制标注来源馆藏，`source_url` 记录出处页面
5. 单馆单日限量、节奏接近人工浏览；失败页面进入人工队列，绝不绕过验证码/反爬机制

**C. 采集合规原则（A/B 两类通用）**

- A 类按官方 API 条款使用；B 类截图方式等同于人工浏览 + 手动保存，不破解、不绕过任何防护
- 每条数据记录 `source_url` 与 `license`/`credit` 字段，全程可溯源
- OCR 文字仅作数据库检索与专家解读素材，成稿一律重新撰写，不照抄原文
- 敏感来源宁可缺数据也不越界采集

### 3.2 数据 Schema（草案）

每件文物一条 JSON 记录（`data/relics/{relic_id}.json`）：

```jsonc
{
  "relic_id": "VGD-000001",          // 本库统一编号
  "name": " hunters",                 // 名称
  "aliases": [],                      // 别名/旧称
  "dynasty": "商晚期",                 // 朝代/年代（统一词表）
  "year_range": [-1300, -1046],       // 数值化年代区间，便于时间轴检索
  "category": "青铜器",               // 大类（统一词表：陶/瓷/玉/青铜/书画…）
  "material": "青铜",
  "dimensions": "通高133厘米",
  "excavated_from": "河南安阳殷墟",    // 出土/来源地
  "collection": {                     // 收藏信息
    "museum": "中国国家博物馆",
    "region": "中国大陆",              // 大陆/港/台/海外-日/海外-欧美…
    "inventory_no": ""
  },
  "summary": "",                      // 100字内基本信息
  "history": "",                      // 历史背景介绍（长文）
  "interpretation": [],               // 学术解读要点（可多条、可带出处）
  "images": [ { "url": "", "license": "CC0", "credit": "" } ],
  "source_url": "",
  "license": "CC0",
  "tags": ["礼器", "祭祀", "酒器"],    // 主题标签（统一词表）
  "related": ["VGD-000002"],          // 人工/算法标注的关联文物
  "fetched_at": "2026-09-04",
  "updated_at": "2026-09-04"
}
```

辅助文件：

- `data/vocab/*.json`：朝代、类别、地区、主题标签的**受控词表**（保证聚合数据口径一致）
- `data/museums.json`：博物馆名录及其数据源配置
- `sqlite/relics.db`：定期生成的 SQLite 快照（供前端与 Agent 快速检索）
- `search-index/`：预构建的全文检索索引

### 3.3 数据处理流水线

1. **采集器（per-source collector）**：每个数据源一个独立适配器，互不影响——A 类走 API，B 类走浏览器截图 + OCR；输出原始 JSON 与截图文件（`raw/`）
2. **清洗归一**：字段映射到统一 Schema；朝代/类别归一到受控词表；繁简转换
3. **去重**：以 `馆藏编号 + 名称 + 收藏机构` 生成指纹；跨馆重复件（如流散文物）用 `related` 关联
4. **标注**：规则 + LLM 辅助打主题标签（如"战争""饮食""女性""货币"），标签是选题Agent检索的关键
5. **质检**：必填字段完整性、图片链接可达性、授权字段非空；不合格进 `quarantine/` 待人工处理
6. **发布**：通过 GitHub Actions 定期构建 SQLite 快照与检索索引并提交

### 3.4 存储方案（重要决策点）

| 方案 | 优点 | 缺点 |
|---|---|---|
| 图片外链（A 类默认） | 仓库轻、无版权风险 | 依赖对方站点稳定性 |
| B 类截图本地保存（jpg 压缩后入库） | 大图可用、不依赖对方站 | 增大仓库体积，需监控 |
| CC0 图片本地缓存 + GitHub LFS | 稳定可控 | LFS 配额有限（1GB 免费） |

**结论**：A 类只存图片 URL + 授权信息；B 类截图压缩为 jpg 存 `data/images/{relic_id}/`（带水印、标来源），设仓库体积警戒线（如 2GB），超限后将图片目录拆分到单独的图片仓库。前端与文章**只引用带来源标注的图片**。

---

## 4. 子系统二：GitHub 仓库与检索前端

### 4.1 仓库结构（规划）

```
52-文物数据库/
├── 工作规划.md
├── collectors/          # 各数据源采集脚本（每馆一个）
├── pipeline/            # 清洗、去重、标注、质检、构建索引
├── data/
│   ├── relics/          # 文物 JSON（一物一文件）
│   ├── vocab/           # 受控词表
│   └── museums.json
├── raw/                 # 原始抓取数据（可 gitignore 部分）
├── sqlite/relics.db     # 构建产物
├── search-index/        # 前端检索索引（构建产物）
├── web/                 # 检索前端源码
├── agents/              # Agent 流水线代码
│   ├── topic_agent/     # 选题
│   ├── expert_agent/    # 文物专家
│   ├── editor_agent/    # 编辑
│   └── knowledge/       # RAG 知识库索引（文本不入库，只入索引）
├── output/articles/     # 每日产出的推文（MD + 图片清单）
└── .github/workflows/   # 定时任务：增量采集 / 索引构建 / 每日选题成稿
```

### 4.2 检索前端（一期功能从简）

- 技术形态：**纯静态站**（GitHub Pages），无需后端
- 技术选型建议：Astro 或纯 HTML+JS；检索用 **Pagefind 或 MiniSearch**（预构建索引，浏览器端查询，中文分词需验证）
- 核心功能（一期）：
  - 关键词搜索（名称/别名/简介全文）
  - 筛选：朝代、类别、收藏博物馆、地区、主题标签
  - 文物详情页：图 + 基本信息 + 历史介绍 + 来源与授权声明
  - 时间轴浏览（按 year_range）
- 二期增强：文物组合浏览（按 related 关系成组展示）、随机"今日文物"

---

## 5. 子系统三：Agent 内容流水线（核心创新）

### 5.1 总体流程（每日一次，人工过目后发布）

```
        ┌────────────────────────────────────────────┐
        │ ⓪ 总协调Agent（Orchestrator，全程调度与状态管理）│
        └────────────────────────────────────────────┘
①热点采集 → ②选题Agent（候选选题+文章结构）
      →（可选：人工选定）→ ③文物专家Agent（选文物组合+专业意见）
      → ④编辑Agent（成稿：公众号版+小红书版）→ ⑤审校/质检
      → ⑥人工过目 → ⑦归档 output/articles/
```

### 5.2 总协调 Agent（Orchestrator，新增）

流水线的"总指挥"，本身不产出内容，负责让整条链路自动跑通、可观测、可恢复：

- **流程控制**：按上述顺序调度各子 Agent，控制每一步的输入/输出传递
- **进度管理**：维护每日流水线状态机（待采集→选题中→专家意见中→成稿中→待人工过目→已归档），落盘为 `output/{date}/pipeline-state.json`，中断后可从断点续跑
- **产物定位**：统一管理各环节产出物路径（热点简报、选题简报、专业意见、成稿、图片清单），任何一步的产出可被下游与人工快速找到
- **异常处理**：子 Agent 失败自动重试（上限 2 次）；选题被数据库支撑度校验淘汰时自动换下一个候选；连续失败则告警并终止当日任务
- **每日运行报告**：任务结束输出一份摘要（当日选题、耗时、积分消耗、各环节产物清单），方便人工过目时总览

### 5.3 选题 Agent

**输入**：每日热点信号 + 数据库标签统计
**热点源（候选，逐个评估接入难度）**：

- 社媒热榜：微博热搜、知乎热榜、抖音热点、B站热门
- 日历信号：历史today、传统节气、纪念日、影视剧上映（如历史剧带火文物）
- 常驻选题库：预设的经典主题（饮食、战争、爱情、货币、刑罚、服饰…），热点平淡时兜底

**职责**：

1. 从热点中提炼**有人性张力、能古今对照**的候选选题（每日 3–5 个）
2. 每个候选输出《选题简报》：选题名、热点依据、拟反映的历史状态/事件、目标情绪基调、文章结构提纲（起承转合）
3. 用主题标签查数据库，预判"该选题下有无足够文物支撑（≥3件且类型多样）"，**无支撑的选题直接淘汰**——这是与数据库强耦合的关键校验

**输出**：选定 1 个选题 + 结构提纲 → 交给文物专家Agent

### 5.4 文物专家 Agent

**人设**：兼具历史学与考古学训练的博物馆研究部研究员。

**知识库（RAG，本地私用、不公开分发）**：

| 知识库 | 素材（候选，最终书目待定） | 处理方式 |
|---|---|---|
| 历史学 | 剑桥中国史系列、讲谈社·中国的历史系列等 | PDF→文本→切块→embedding→向量库 |
| 考古学 | 考古学通论类教材、各朝重要考古报告综述、大百科·考古学卷等 | 同上，**独立命名空间**（历史≠考古，检索时两路召回再融合） |
| （预留）| 艺术史、科技史、物质文化史 | 二期扩展 |

**首批书目调研 —— 交由 `45-文献助手` Agent 完成**（`E:\codingProject\45-文献助手`，设计文档：文献助手Agent设计文档.md）：

- 调研方式：用其**穷尽式论文/文献检索**（ScholarSearcher，场景 C）+ **荐读**（ReadingAdvisor，场景 B）能力，按"面向大众历史内容创作的知识库选书"目标，产出历史学、考古学两个方向的推荐书目清单（含入选理由、优先级、版本建议）
- 注意：文献助手当前状态为**设计稿 v1.0 + 部分工具**（tools/parse_book.py、build_epub.py 等），P2 启动前需确认其可用性；若核心模块未实现，则先由文献助手设计文档中的方法论指导人工调研，或用通用研究 agent 过渡
- 后续衔接：文献助手的 BookReader（场景 A，全书通读出章节重点卡片）可反哺知识库构建——用"章节重点卡片"替代纯机械切块，提升 RAG 质量；其防幻觉体系（claim-evidence 绑定）与本项目专家 Agent 的溯源要求天然契合

- 版权注意：知识库仅本地自用，向量索引不进公开仓库（gitignore + 本地构建）

**职责**（两步走）：

1. **选文物组合**：根据选题提纲检索数据库（标签 + 年代 + 类别），选出 **3–6 件**能组成叙事的文物组合，并说明组合逻辑（如"一组汉代饮食文物：灶台模型+耳杯+厨俑，还原宴饮场景"）。要求组合有时间纵深或空间对比（如商周 vs 唐，中原 vs 边疆）
2. **撰写《专业意见》**：
   - 每件文物：名称考订、年代判定、功能与使用场景、出土背景、学术界的不同观点（若有）
   - 组合整体：共同还原的历史图景、与热点的古今对照点、人性层面的引申（点到为止，留给你编辑发挥）
   - **事实性红线**：所有史实论断标注知识库出处；知识库无据的写"存疑"或删去

**输出**：《专业意见》文档 + 选定文物清单（含图片 URL 与授权信息）

### 5.5 编辑 Agent

**输入**：选题简报 + 专业意见 + 文物图文素材
**职责**：写成一篇**有意思的图文推文**，且**一次产出双平台版本**（发布渠道已确认为公众号 + 小红书）：

- 结构遵循选题提纲；开头 3 句内勾住读者（从热点切入）
- 文物介绍"去论文腔"：以场景与故事讲文物，专业术语随文解释
- 古今对照自然不生硬；人性探讨做收束升华
- 每件文物配图，图片下方标注馆藏与来源信息（合规硬要求）
- 文末附"参考资料"（文物来源链接 + 知识库书目）
- **双平台输出**：
  - `公众号版`：**HTML 排版稿（已确认）**——内联样式的完整 HTML，可直接粘贴进公众号编辑器；文末参考资料完整
  - `小红书版`：≤1000 字笔记体（口语化、分段短、带话题标签），标题 ≤20 字，配图按小红书 3:4 比例从文物大图裁切的建议清单
  - 同时保留 Markdown 原稿作为可编辑母版
- 输出到 `output/articles/YYYY-MM-DD-标题/`（长文.md、小红书.md、图片清单.json）

**风格约束**：将沉淀一份《编辑风格手册》（语调、句长、禁用词、梗的使用边界、双平台差异），供编辑Agent与未来人工校准。

### 5.6 审校与质检（保留环节）

- 自动质检：事实性抽查（关键年代/人名/馆藏地与数据库回核对验）、图片来源标注完整性、字数与结构完整性、双平台版本一致性
- **已确认：每日产出一篇，人工过目后再发布**（公众号/小红书手动发布）；流水线跑顺后再评估是否放宽

### 5.7 调度与自动化

- GitHub Actions cron：夜间增量采集 → 每周重建索引 → 每日选题成稿流程
- **预算控制（已确认：GLM-5.3-Flash，每周 1 万积分）**：
  - 每日积分限额约 1,400，由总协调Agent记录各环节消耗并在运行报告中汇总
  - 分级消耗策略：热点采集、标签标注等常规任务用低提示量批量调用；专家意见与成稿环节优先保障预算；超额时自动降级为"只出选题简报、次日成稿"
  - 采集/OCR 等步骤零 LLM 消耗（纯本地），LLM 只花在选题、专家、编辑三处
- 失败告警：workflow 失败通知（如邮件/企业微信/webhook）
- 全流程产物落仓库，可追溯、可回滚

---

## 6. 实施路线图

### P0：项目奠基 + 数据库 MVP（预计 2–3 周）—— 进行中

- [x] 确定技术栈、初始化 Git 仓库（2026-09-04）
- [x] 定稿数据 Schema 与受控词表 v1（schemas/relic.schema.json + data/vocab/*，2026-09-04）
- [x] 接入 Met（大都会）Open Access：**2,220 件已入库**（validate 全部合格，476 件待复核朝代）；克利夫兰、台北故宫待接入
- [x] 采集→质检→建库脚本跑通（collectors/met_collector.py、pipeline/validate.py、pipeline/build_db.py）
- [ ] GitHub Actions 定时任务实测（工作流已写好 `.github/workflows/update-data.yml`，待仓库推送到 GitHub 后验证）
- [ ] 去重指纹模块（单源阶段优先级低，接第二源前完成）
- [ ] LLM 辅助主题标签标注（当前为规则打标，覆盖面有限）

**验收进展**：仓库中可浏览 JSON 数据（2,220 件）；一条命令完成增量采集与发布（本机验证通过）。

### P1：检索前端（预计 1–2 周）

- [ ] 静态站 + 中文全文检索 + 筛选 + 详情页 + 时间轴
- [ ] GitHub Pages 上线

**验收**：普通人能用网页搜到"司母戊鼎/毛公鼎"等文物并看到图与介绍。

### P2：专家Agent + 知识库（预计 2–3 周）

- [ ] 前置：用 `45-文献助手` 产出首批书目清单（历史学 + 考古学），人工确认后采购/收集 PDF
- [ ] 历史学、考古学知识库构建（转文本、切块、向量化；探索用文献助手 BookReader 章节重点卡片替代机械切块）
- [ ] 专家Agent：文物组合选择 + 专业意见生成
- [ ] 标注体系完善（主题标签质量决定选品质量，重点打磨）

**验收**：给定任意选题，能产出一份引用可溯源的《专业意见》与合理文物组合。

### P3：总协调Agent + 选题/编辑Agent + 每日流水线（预计 2 周）

- [ ] 热点源接入与选题简报生成
- [ ] 编辑Agent成稿（公众号版 + 小红书版）+ 风格手册 v1
- [ ] 总协调Agent：状态机、断点续跑、积分预算控制、每日运行报告
- [ ] 全流水线串联 + 每日定时运行 + 失败告警 + 人工过目发布流程

**验收**：连续 7 天自动产出 7 篇合格推文（人工抽检事实错误率 < 1处/篇）。

### P4：优化与扩展（长期）

- 扩充数据源（大陆无API大馆、港台补充源），目标 **≥50,000 件**
- 文物组合关系库（related 人工/算法双轨完善）、组合浏览前端
- 审校Agent、多平台发布适配（公众号/小红书排版）
- 读者反馈回路（哪类选题阅读好 → 反哺选题Agent）

---

## 7. 技术选型建议（供确认）

| 组件 | 建议 | 备选 |
|---|---|---|
| 语言 | Python（采集/流水线/Agent 均生态最好） | Node.js |
| LLM | **GLM-5.3-Flash**（已确认），预算每周 1 万积分 | 积分不足时降级或错峰 |
| B 类采集 | Playwright（有头浏览器实开网页 + 截图） | 人工半自动截图（过渡期） |
| OCR | RapidOCR（本机 document-extraction 技能同款） | PaddleOCR |
| RAG | 本地向量库（如 Chroma / sqlite-vec） | LangChain 全家桶（偏重） |
| 前端 | 静态站 + Pagefind/MiniSearch | Astro + Pagefind |
| 调度 | GitHub Actions + 本机计划任务（B 类截图需真实浏览器环境，宜本机跑） | — |

---

## 8. 风险与合规清单

| 风险 | 等级 | 对策 |
|---|---|---|
| 图片版权 | 中 | A 类只用 CC0/开放授权图；B 类截图带水印、图注强制标明来源馆藏；每图带 license/credit 字段 |
| 大陆大馆无 API、有反爬 | 高 | 采用"浏览器实开 + 截图 + OCR"方式（等同人工浏览）；一期先靠海外+台北故宫 Open Data 起量 |
| OCR 识别错误污染数据库 | 中 | OCR 结果抽样人工校对；关键字段（名称/年代/馆藏号）与已知目录交叉验证；置信度低的字段标记待核 |
| 浏览器自动化稳定性 | 中 | 页面改版检测 + 失败进人工队列；单馆单日限量 |
| 知识库书籍版权 | 中 | 仅本地自用、不公开分发；仓库不含原书文本与向量索引 |
| 史实幻觉 | 高 | 专家Agent强制溯源标注；审校环节与数据库回核对验；每日人工过目 |
| GitHub 仓库体积（截图入库） | 中 | 截图压缩为 jpg；设体积警戒线，超限拆分图片仓库 |
| 各馆数据口径不一 | 中 | 受控词表 + 归一化层，宁缺毋错 |
| 热点源接口变动/反爬 | 中 | 热点源做成可插拔适配器；常驻选题库兜底 |
| 积分预算超限（周 1 万） | 中 | 总协调Agent记账 + 每日限额；超额自动降级为只出选题简报 |
| 内容平台合规（历史类推文） | 中 | 避免敏感史观表述；风格手册中设红线清单 |

---

## 9. 已确认决策（2026-09-04）

| # | 决策项 | 结论 |
|---|---|---|
| 1 | 发布渠道 | **公众号 + 小红书**（编辑Agent一次产出双平台版本；人工手动发布） |
| 2 | 大模型 | **GLM-5.3-Flash** |
| 3 | 预算上限 | **每周 1 万积分**（约每日 1,400，由总协调Agent记账控额） |
| 4 | 知识库首批书目 | **由 `45-文献助手` Agent 调研产出**（穷尽式检索 + 荐读；该 Agent 位于 `E:\codingProject\45-文献助手`，当前为设计稿 v1.0 + 部分工具，P2 启动前确认可用性） |
| 5 | 产出节奏 | **每日 1 篇，人工过目后发布** |
| 6 | 开源策略 | **项目公开开源到 GitHub**（知识库原书与向量索引除外，仅本地） |
| 7 | B 类试点馆 | **中国国家博物馆官网**（截图 + OCR 采集方法论在此跑通） |
| 8 | 公众号排版 | **HTML**（内联样式，可直接粘贴公众号编辑器，不依赖第三方排版工具） |
| 9 | GitHub 仓库名 | **`wenwu-database`**（本地目录仍为 `52-文物数据库`） |

**开工后待定小项**：

- 小红书封面图生成方式
- GitHub 组织/账号归属（先推个人账号）

---

*本文档为规划稿，评审通过后按 P0 → P3 顺序推进；每阶段结束更新本规划的状态列。*
